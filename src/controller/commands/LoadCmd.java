package controller.commands;

import controller.ImageProcessorCommand;
import model.Image;
import model.StoredImages;

public class LoadCmd implements ImageProcessorCommand {

  public LoadCmd(Image image, String fileName) {

  }

  /**
   * @param storedImages
   */
  @Override
  public void execute(StoredImages storedImages) {

  }
}
